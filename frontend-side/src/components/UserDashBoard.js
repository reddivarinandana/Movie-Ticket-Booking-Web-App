import React from "react";

const UserDashboard = ({ token }) => {
  return (
    <div>
      <h2>User Dashboard</h2>
      {/* Placeholder - add movie listing & booking here next */}
    </div>
  );
};

export default UserDashboard;
